@model SimpleBlog.FrontEnd.ViewModels.PostsViewModel
@{
    ViewData["Title"] = "Our Simple Blog Posts";
}

<h1>Our Posts</h1>
@if(Model.Posts != null && Model.Posts.Count() > 0) 
{
    foreach(var post in Model.Posts) 
    {
        <div class="row m1emb">
            <a href="@Url.Action("Details", "Posts", new { id = post.Id })">
                <div class="col-md-2">
                    <img src="@post.Image" alt="@post.Title" class="img-responsive" />
                </div>
               
                <div class="col-md-10">
                    <h3>@post.Title</h3>
                     <h4>  <span class="glyphicon glyphicon-comment" aria-hidden="true"></span> @post.CommentCount </h4>

                </div>
            </a>
        </div>
    }
} else {
    <div class="row">
        <div class="col-md-12">
            <p>There's nothing to see here.. :|</p>
        </div>
    </div>
}